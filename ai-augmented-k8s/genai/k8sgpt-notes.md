K8sGPT Notes

- K8sGPT can analyze manifests, logs, and kubectl outputs to suggest fixes.
- Install as a CLI or run as a pod that has access to cluster logs.
- Provide your OpenAI API key via environment variable when using the CLI: `export OPENAI_API_KEY=..`.